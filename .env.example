# Copy to .env and fill in. Never commit .env.
GEMINI_API_KEY=your-gemini-api-key-here
HMAC_SECRET=generate-with-openssl-rand-hex-32
# Set to 1 to test fallback questions/vibe while Gemini is configured.
FORCE_FALLBACKS=1
